#!groovy

pipeline {
    agent { node ('') /*where to run this */}
        // wait condition
        // Input paramter pro stage
    parameters {
        string(name: '', defaultValue: '', description: '')
    }
    stages {
        stage('') {
            steps {
                // What can COZMO do?
            }
        }

    }
    post {
        always {
            // Post Actions
        }
    }
}